# Native dependency used by CairoSVG/cairocffi on macOS.
# Homebrew does not provide an immutable historical-version lock. The
# toolchain check records and enforces the version validated for this system.
brew "cairo"
